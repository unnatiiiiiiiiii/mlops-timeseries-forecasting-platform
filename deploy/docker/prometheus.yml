global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "metrics_exporter"
    static_configs:
      - targets: ["metrics-exporter:9100"]

  - job_name: "api"
    static_configs:
      - targets: ["api:8000"]
